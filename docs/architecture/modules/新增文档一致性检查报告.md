# 新增模块文档一致性检查报告

检查时间：2026-07-07
检查对象：
- `docs/architecture/modules/13-知识加工层.md`
- `docs/architecture/modules/14-索引存储层.md`

参照文档：
- `docs/design.md`
- `docs/architecture/backend/06-数据模型设计.md`
- `docs/architecture/backend/08-RAG功能层次与状态机.md`
- `backend/app/db/models.py`（实际代码）

---

## 一、发现的问题

### 🔴 严重问题

#### 问题1：字段名不一致 - `clause` vs `clause_number`

**位置**：`14-索引存储层.md`

**不一致点**：
- 我的文档使用：`clause_number`
- 实际数据库字段：`clause`（见 `06-数据模型设计.md` 和 `backend/app/db/models.py`）

**出现位置**：
1. 第 4.1 节 MySQL 表结构示例（line ~250）
2. 第 4.2 节精确查询场景（line ~280）

**修复方案**：
```python
# 错误（我的文档）
chunk = db.query(Chunk).join(Document).filter(
    Document.standard_no == "GB 50057-2010",
    Chunk.clause_number == "4.3.2"  # ❌ 应该是 clause
).first()

# 正确
chunk = db.query(Chunk).join(Document).filter(
    Document.standard_no == "GB 50057-2010",
    Chunk.clause == "4.3.2"  # ✅
).first()
```

---

### 🟡 中等问题

#### 问题2：Payload 字段命名不完全对齐

**位置**：`14-索引存储层.md` 第 2.2 节

**不一致点**：
- 我的文档使用：`clause_number` (Qdrant Payload)
- 实际字段应该对齐 MySQL：`clause`

**影响**：
- Qdrant Payload 字段名应与 MySQL 字段名保持一致，便于过滤查询

**修复方案**：
```python
# Payload 字段统一为 "clause"
payload = {
    "clause_number": "4.3.2",  # ❌
    "clause": "4.3.2",          # ✅
}
```

---

### 🟢 轻微问题

#### 问题3：chunks 表字段遗漏

**位置**：`14-索引存储层.md` 第 4.1 节 MySQL 表结构

**遗漏字段**：
- `content_hash` (VARCHAR(64), UNIQUE) - 内容去重
- `has_dense_vector` (BOOLEAN) - 是否有稠密向量
- `has_sparse_vector` (BOOLEAN) - 是否有稀疏向量
- `related_chunk_ids` (JSON) - 关联块IDs
- `token_count` (INT) - Token数量
- `char_count` (INT) - 字符数

**影响**：轻微，示例性质，但不够完整

**修复方案**：补充完整字段定义或添加"详见 06-数据模型设计.md"的引用

---

## 二、正确性验证

### ✅ 正确的部分

#### 1. 三库协同架构 - 完全对齐

- Qdrant + Elasticsearch + MySQL 的定位 ✅
- 向量库存储父块、ES存储子块的设计 ✅
- MinIO 存储原始PDF ✅

#### 2. 父子块机制 - 与 design.md 一致

- 父块：512~1024 token ✅
- 子块：128~256 token ✅
- 检索流程：先召回父块，再定位子块 ✅

#### 3. 向量化双向量机制 - 完全正确

- 稠密向量：bge-large-zh-v1.5, 1024维 ✅
- 稀疏向量：BM42/SPLADE ✅
- Qdrant 原生混合检索（RRF融合）✅

#### 4. 元数据体系 - 与 design.md 对齐

- 基础属性、分类属性、位置属性、权重属性 ✅
- 所有字段与 `06-数据模型设计.md` 匹配 ✅

#### 5. 数据处理流程 - 与 design.md 一致

```
原始PDF → 版面分析 → 文本提取 → 公式识别 → 
表格还原 → 结构重建 → 清洗归一
```
完全对齐 ✅

#### 6. 智能分块策略 - 准确

- 国家标准按条款粒度拆分 ✅
- 书籍按语义知识点分块 ✅
- 公式完整性保护 ✅
- 引用关联处理 ✅

---

## 三、修复清单

### 必须修复（影响代码实现）

- [ ] **14-索引存储层.md 第 4.1 节**：`clause_number` → `clause`
- [ ] **14-索引存储层.md 第 4.2 节**：查询示例中 `clause_number` → `clause`
- [ ] **14-索引存储层.md 第 2.2 节**：Payload 示例中 `clause_number` → `clause`

### 建议改进（不影响正确性）

- [ ] **14-索引存储层.md 第 4.1 节**：补充 chunks 表完整字段或添加"详见 06-数据模型设计.md"
- [ ] **13-知识加工层.md**：术语标准化章节可补充引用 `term_dictionary` 表结构

---

## 四、跨文档引用检查

### ✅ 引用正确性

**13-知识加工层.md**：
- ✅ 引用 `14-索引存储层.md`
- ✅ 引用 `15-检索引擎层.md`
- ✅ 引用 `17-运维管控层.md`
- ✅ 引用 `22-模型使用总结.md`
- ✅ 引用 `design.md`

**14-索引存储层.md**：
- ✅ 引用 `13-知识加工层.md`
- ✅ 引用 `15-检索引擎层.md`
- ✅ 引用 `06-数据模型设计.md`
- ✅ 引用 `design.md`

---

## 五、总结与建议

### 总体评价

两份文档的**架构设计、技术选型、流程定义**与现有文档**高度一致**，核心内容准确无误。

唯一的严重问题是**字段名不一致**（`clause_number` vs `clause`），这会导致代码实现时的困惑。

### 修复优先级

1. **高优先级**：修复字段名不一致问题（3处）
2. **中优先级**：补充完整的表结构定义
3. **低优先级**：优化示例代码的完整性

### 修复后可立即合并

修复字段名问题后，这两份文档质量高、内容准确、结构清晰，可以作为标准模块文档使用。

---

生成于：2026-07-07
检查工具：Claude Code
