# 父块子块检索优化方案

## 背景

### 当前问题

从 `recall.py` 代码分析发现，当前检索流程存在以下问题：

1. **召回阶段只返回父块**：`_search_by_standard()` 中过滤 `chunk_type == 'parent'`（第 465、475 行）
2. **缺少子块扩展环节**：重排后没有获取父块关联的子块
3. **生成时上下文粗糙**：只有父块的大段文本，无法精准定位到关键细节

### 为什么需要父块子块检索

**父块（parent chunk）**：
- 大块文本（500-1000 tokens），包含完整语义单元（章节、条款）
- 优点：召回时语义完整，不易丢失上下文
- 缺点：过长，生成时可能淹没关键信息

**子块（child chunk）**：
- 小块文本（100-200 tokens），细粒度切分
- 优点：精准匹配用户查询的具体细节
- 缺点：召回时语义不完整，易丢失上下文

**最佳实践**：
1. 召回时用父块（保证语义完整性）
2. 重排后扩展子块（获取精细匹配）
3. 生成时用"父块上下文 + 高相关子块"

---

## 设计方案

### 架构设计

```
┌─────────────────────────────────────────────────────────────┐
│                     三路并行召回                              │
│  Vector(20) + Keyword(20) + Structured(10) → RRF融合(50)    │
│                   ↓ 只召回父块                               │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      重排层（Rerank）                         │
│        CoarseRerank: 50 → 20（父块粗排）                     │
│        FineRerank:   20 → 10（父块精排）                     │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                 ✨ 新增：子块扩展层 ✨                        │
│  1. 获取 Top-10 父块的关联子块（每个父块最多5个子块）         │
│  2. 用查询向量对子块做相似度计算                             │
│  3. 过滤低相关子块（threshold > 0.7）                        │
│  4. 返回"父块 + 高相关子块列表"                              │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      生成层（Generation）                     │
│  上下文格式：                                                │
│    - 父块完整内容（提供语义上下文）                           │
│    - 标注高相关子块（用【重点】标记）                         │
│  生成时 LLM 重点关注【重点】标记的子块                        │
└─────────────────────────────────────────────────────────────┘
```

### 数据模型支持

数据库已支持父子关系（`app/db/models.py`）：

```python
class Chunk(Base):
    chunk_type = Column(String(20))  # 'parent' | 'child'
    parent_chunk_id = Column(Integer, ForeignKey('chunks.id'))  # 子块指向父块
    children = relationship('Chunk', backref=backref('parent', remote_side=[id]))
```

Qdrant 向量库中：
- 父块和子块都有独立的向量
- payload 中包含 `chunk_type` 和 `parent_chunk_id` 字段

---

## 实现方案

### 1. 新增子块扩展模块

**文件位置**：`backend/app/core/retrieval/child_expander.py`

```python
"""
子块扩展层 (Child Chunk Expander)

功能：
1. 批量获取父块关联的子块（避免 N+1 查询）
2. 优先使用 Qdrant 已存储的向量，缺失时才重新计算
3. 用查询向量对子块做相似度计算
4. 过滤低相关子块
5. 返回"父块 + 高相关子块"的混合结果
"""
from typing import List, Dict, Optional
from sqlalchemy.orm import Session
import numpy as np
import logging

from app.db.models import Chunk
from app.schemas.retrieval import ChunkResult, ExpandedChunkResult
from app.core.embedding.embedder import Embedder
from app.storage.cache import get_cache_manager
from app.storage.vector_store import VectorStore

logger = logging.getLogger(__name__)


class ChildChunkExpander:
    """子块扩展器"""

    def __init__(self, db: Session):
        self.db = db
        self.embedder = Embedder()
        self.cache = get_cache_manager()
        self.vector_store = VectorStore()

    async def expand(
        self,
        parent_chunks: List[ChunkResult],
        query: str,
        similarity_threshold: float = 0.7,
        max_children_per_parent: int = 5
    ) -> List[ExpandedChunkResult]:
        """
        扩展父块的子块

        Args:
            parent_chunks: 重排后的父块列表
            query: 用户查询
            similarity_threshold: 子块相似度阈值（低于此值的子块不返回）
            max_children_per_parent: 每个父块最多保留的子块数

        Returns:
            List[ExpandedChunkResult]: 包含父块和高相关子块的结果
        """
        if not parent_chunks:
            return []

        # 生成查询向量（用于计算子块相似度）
        query_vector = self.embedder.encode(query)

        # ✅ 修复 1：批量获取所有父块的子块（一次查询，避免 N+1）
        parent_ids = [p.chunk_id for p in parent_chunks]
        children_map = self._get_children_batch(parent_ids)

        # 收集所有子块用于批量向量获取
        all_children = [child for children_list in children_map.values() 
                        for child in children_list]

        if not all_children:
            # 所有父块都没有子块
            return [ExpandedChunkResult(parent=p, relevant_children=[]) 
                    for p in parent_chunks]

        # ✅ 修复 3：优先从 Qdrant 获取已存储的向量，减少重复计算
        child_vectors = await self._get_child_vectors_batch(all_children)

        # 处理每个父块
        results = []
        total_children = 0
        filtered_children = 0
        cache_hits = 0

        for parent in parent_chunks:
            children = children_map.get(parent.chunk_id, [])
            if not children:
                results.append(ExpandedChunkResult(
                    parent=parent,
                    relevant_children=[]
                ))
                continue

            total_children += len(children)

            # 计算子块相似度
            child_scores = []
            for child in children:
                child_vector = child_vectors.get(child.id)
                if child_vector is None:
                    logger.warning(f"No vector for child {child.id}, skipping")
                    continue

                if child_vector.get('from_cache'):
                    cache_hits += 1

                # 余弦相似度
                similarity = float(np.dot(query_vector, child_vector['vector']))
                child_scores.append((child, similarity))

            # 过滤 + 排序
            relevant_children = [
                (child, score) for child, score in child_scores
                if score >= similarity_threshold
            ]
            relevant_children.sort(key=lambda x: x[1], reverse=True)

            # 限制数量
            relevant_children = relevant_children[:max_children_per_parent]
            filtered_children += len(relevant_children)

            # 转换为 ChunkResult
            child_results = [
                self._to_chunk_result(child, score, parent)
                for child, score in relevant_children
            ]

            results.append(ExpandedChunkResult(
                parent=parent,
                relevant_children=child_results,
                expansion_stats={
                    'total_children': len(children),
                    'filtered_children': len(child_results),
                    'avg_score': np.mean([s for _, s in relevant_children]) if relevant_children else 0.0
                }
            ))

        # 监控指标
        cache_hit_rate = cache_hits / total_children if total_children > 0 else 0
        logger.info(
            f"[ChildExpander] Stats: parents={len(parent_chunks)}, "
            f"total_children={total_children}, filtered_children={filtered_children}, "
            f"avg_children_per_parent={filtered_children / len(parent_chunks):.1f}, "
            f"cache_hit_rate={cache_hit_rate:.2%}"
        )

        return results

    def _get_children_batch(self, parent_ids: List[int]) -> Dict[int, List[Chunk]]:
        """
        ✅ 修复 1：批量获取多个父块的子块（避免 N+1 查询）
        
        Args:
            parent_ids: 父块 ID 列表
            
        Returns:
            Dict[parent_id, List[Chunk]]: 父块 ID → 子块列表的映射
        """
        if not parent_ids:
            return {}

        try:
            children = (self.db.query(Chunk)
                        .filter(Chunk.parent_chunk_id.in_(parent_ids),
                                Chunk.chunk_type == 'child')
                        .order_by(Chunk.parent_chunk_id, Chunk.position_in_doc)
                        .all())

            # 按父块 ID 分组
            result = {}
            for child in children:
                result.setdefault(child.parent_chunk_id, []).append(child)

            logger.info(f"[ChildExpander] Fetched {len(children)} children for {len(parent_ids)} parents")
            return result

        except Exception as e:
            logger.error(f"Failed to batch get children: {e}", exc_info=True)
            return {}

    async def _get_child_vectors_batch(
        self,
        children: List[Chunk]
    ) -> Dict[int, Dict[str, any]]:
        """
        ✅ 修复 2 & 3：批量获取子块向量（优先缓存 → Qdrant → 重新计算）
        
        Args:
            children: 子块列表
            
        Returns:
            Dict[chunk_id, {'vector': np.ndarray, 'from_cache': bool}]
        """
        result = {}

        # 第一步：从缓存获取
        children_need_fetch = []
        for child in children:
            # ✅ 修复 2：使用 chunk_id 作为缓存键，避免内容冲突
            cached_vector = self.cache.get_dense_by_id(child.id)
            if cached_vector is not None:
                result[child.id] = {'vector': cached_vector, 'from_cache': True}
            else:
                children_need_fetch.append(child)

        if not children_need_fetch:
            return result

        # 第二步：从 Qdrant 获取已存储的向量
        children_with_vector_id = [c for c in children_need_fetch if c.vector_id]
        children_need_compute = [c for c in children_need_fetch if not c.vector_id]

        if children_with_vector_id:
            stored_vectors = await self._batch_get_vectors_from_qdrant(children_with_vector_id)
            for child_id, vector in stored_vectors.items():
                result[child_id] = {'vector': vector, 'from_cache': False}
                # 写入缓存供后续使用
                self.cache.set_dense_by_id(child_id, vector)

        # 第三步：批量计算缺失的向量
        if children_need_compute:
            contents = [c.content for c in children_need_compute]
            new_vectors = self.embedder.batch_encode(contents)
            for child, vector in zip(children_need_compute, new_vectors):
                result[child.id] = {'vector': vector, 'from_cache': False}
                # 写入缓存
                self.cache.set_dense_by_id(child.id, vector)

        return result

    async def _batch_get_vectors_from_qdrant(
        self,
        children: List[Chunk]
    ) -> Dict[int, np.ndarray]:
        """
        从 Qdrant 批量获取子块向量
        
        Args:
            children: 有 vector_id 的子块列表
            
        Returns:
            Dict[chunk_id, vector]
        """
        try:
            vector_ids = [c.vector_id for c in children]
            points = self.vector_store.client.retrieve(
                collection_name="documents",
                ids=vector_ids,
                with_vectors=True
            )

            result = {}
            id_to_chunk = {c.vector_id: c for c in children}

            for point in points:
                chunk = id_to_chunk.get(point.id)
                if chunk and point.vector:
                    # Qdrant 返回的 vector 可能是 dict (稠密+稀疏)，取 dense
                    if isinstance(point.vector, dict):
                        vector = np.array(point.vector.get('dense', []), dtype=np.float32)
                    else:
                        vector = np.array(point.vector, dtype=np.float32)
                    result[chunk.id] = vector

            logger.info(f"[ChildExpander] Fetched {len(result)} vectors from Qdrant")
            return result

        except Exception as e:
            logger.error(f"Failed to batch get vectors from Qdrant: {e}", exc_info=True)
            return {}

    def _to_chunk_result(
        self,
        chunk: Chunk,
        score: float,
        parent: ChunkResult
    ) -> ChunkResult:
        """将 Chunk 转换为 ChunkResult"""
        return ChunkResult(
            chunk_id=chunk.id,
            document_id=chunk.document_id,
            content=chunk.content,
            score=score,
            # 继承父块的文档信息
            document_title=parent.document_title,
            standard_no=parent.standard_no,
            doc_type=parent.doc_type,
            category=parent.category,
            voltage_level=parent.voltage_level,
            clause=chunk.clause,
            chapter=chunk.chapter,
            section=chunk.section,
            page_start=chunk.page_start,
            page_end=chunk.page_end,
            recall_source="child_expanded"
        )
```

### 2. 新增数据模型

**文件位置**：`backend/app/schemas/retrieval.py`（新增）

```python
from typing import List
from pydantic import BaseModel

class ExpandedChunkResult(BaseModel):
    """扩展后的检索结果（父块 + 子块）"""
    parent: ChunkResult
    relevant_children: List[ChunkResult]  # 高相关子块，按相似度降序
```

### 3. 集成到检索流程

**修改文件**：`backend/app/core/retrieval/fast_lane.py`、`slow_lane.py`

```python
# 在 FastLane.search() 中，重排后增加子块扩展
from app.core.retrieval.child_expander import ChildChunkExpander

async def search(self, query_input: QueryInput) -> QueryResult:
    # ... 现有代码：召回 + 重排 ...
    
    # 精排后的 Top-10 父块
    final_chunks = await self.reranker.fine_rerank(
        coarse_chunks, preprocessed.normalized_query, top_k=10
    )
    
    # ✨ 新增：子块扩展
    expander = ChildChunkExpander(self.db)
    expanded_results = await expander.expand(
        parent_chunks=final_chunks,
        query=preprocessed.normalized_query,
        similarity_threshold=0.7,
        max_children_per_parent=5
    )
    
    # 传递给生成层
    answer = await self.generator.generate(
        query=preprocessed.normalized_query,
        expanded_chunks=expanded_results,  # 新参数
        ...
    )
```

### 4. 生成层改造

**修改文件**：`backend/app/core/generation/generator.py`

```python
async def generate(
    self,
    query: str,
    expanded_chunks: List[ExpandedChunkResult],  # 新参数
    ...
) -> str:
    """生成答案"""
    
    # 构建上下文（父块 + 标注高相关子块）
    context_parts = []
    for item in expanded_chunks:
        parent = item.parent
        
        # 父块内容
        context = f"【文档 {parent.standard_no or ''}】\n{parent.content}\n"
        
        # 高相关子块（用【重点】标记）
        if item.relevant_children:
            context += "\n【重点片段】：\n"
            for child in item.relevant_children:
                context += f"- {child.content}\n"
        
        context_parts.append(context)
    
    context_str = "\n---\n".join(context_parts)
    
    # 调用 LLM
    prompt = f"""参考以下文档回答问题。【重点片段】中的内容与问题高度相关，请重点参考。

{context_str}

问题：{query}

要求：
1. 优先使用【重点片段】中的内容
2. 结合父块上下文理解完整语义
3. 标注引用来源
"""
    # ... 调用 LLM ...
```

---

## 性能优化

### 1. 缓存策略

子块向量计算开销较大，需要缓存：

```python
# 在 child_expander.py 中
child_vector = self.cache.get_dense(child.content)
if child_vector is None:
    child_vector = self.embedder.encode(child.content)
    self.cache.set_dense(child.content, child_vector)
```

**缓存命中率预期**：
- 同一标准的子块在不同查询中复用率高
- L1 Embedding 缓存 TTL 设为 24 小时

### 2. 批量向量计算

如果子块较多（> 10 个），使用批量 encode：

```python
# 批量计算相似度
child_contents = [c.content for c in children]
child_vectors = self.embedder.batch_encode(child_contents)  # 新方法
similarities = np.dot(child_vectors, query_vector)
```

### 3. 提前终止

如果前 N 个子块相似度都很低（< 0.5），跳过剩余子块：

```python
low_score_count = 0
for child, score in child_scores:
    if score < 0.5:
        low_score_count += 1
        if low_score_count >= 3:
            break  # 前 3 个都很低，放弃剩余子块
```

---

## 测试验证

### 测试用例 1：精确条款查询

**查询**：`GB 50057-2010 第 5.2.1 条的接地电阻要求`

**预期**：
- 召回父块：第 5.2.1 条完整内容（500 tokens）
- 扩展子块：
  - 子块 1：`接地电阻值不应大于 10Ω`（高相关，score > 0.9）
  - 子块 2：`特殊情况下可放宽至 30Ω`（中等相关，score > 0.7）
- 生成答案：优先引用子块 1，用父块补充上下文

### 测试用例 2：笼统查询

**查询**：`变压器差动保护整定原则`

**预期**：
- 召回父块：差动保护章节（多个父块）
- 扩展子块：每个父块扩展 2-3 个高相关子块
- 生成答案：从多个子块中综合信息

### 测试用例 3：无子块情况

**查询**：`某个没有子块的标准号`

**预期**：
- 召回父块：正常返回
- 扩展子块：`relevant_children = []`
- 生成答案：只用父块内容（降级为当前行为）

### 性能基准

**目标**：
- 子块扩展耗时 < 50ms（10 个父块，每个 5 个子块）
- 缓存命中率 > 60%
- 总检索时间增加 < 10%

---

## 实施计划

### Phase 1：基础功能（1-2 天）
- [ ] 实现 `ChildChunkExpander` 类
- [ ] 新增 `ExpandedChunkResult` Schema
- [ ] 集成到 FastLane 和 SlowLane
- [ ] 改造 Generator 支持扩展结果

### Phase 2：性能优化（1 天）
- [ ] 实现批量向量计算
- [ ] 集成 L1 Embedding 缓存
- [ ] 添加提前终止逻辑

### Phase 3：测试验证（1 天）
- [ ] 编写单元测试（`test_child_expander.py`）
- [ ] 端到端测试（3 个测试用例）
- [ ] 性能基准测试
- [ ] A/B 对比测试（有无子块扩展）

### Phase 4：调优（1 天）
- [ ] 调整相似度阈值（0.7 → 0.65？）
- [ ] 调整子块数量上限（5 → 3？）
- [ ] 优化生成 Prompt（【重点】标记的效果）

---

## 风险与限制

### 风险 1：子块数据不完整

**问题**：部分文档可能没有切分子块，只有父块。

**缓解措施**：
- 扩展器检查 `children` 为空时，降级为只返回父块
- 逐步补全数据：重新 Chunk 历史文档

### 风险 2：相似度阈值不准

**问题**：threshold 设置不当导致子块过多/过少。

**缓解措施**：
- 提供可配置参数（`CHILD_SIMILARITY_THRESHOLD`）
- 记录每次查询的子块过滤统计（日志）
- A/B 测试找最优阈值

### 风险 3：性能回退

**问题**：子块扩展增加 100ms 延迟。

**缓解措施**：
- 强制缓存命中率监控
- 批量计算优化
- 异步并行处理多个父块的子块扩展

---

## 监控指标

### 新增 Metrics

```python
# 在 ChildChunkExpander 中
logger.info(f"[ChildExpander] Stats: "
            f"parents={len(parent_chunks)}, "
            f"total_children={total_children}, "
            f"filtered_children={filtered_children}, "
            f"avg_children_per_parent={filtered_children / len(parent_chunks):.1f}, "
            f"cache_hit_rate={cache_hits / total_children:.2%}, "
            f"elapsed_ms={elapsed_ms}")
```

**告警阈值**：
- `elapsed_ms > 100`：子块扩展过慢
- `cache_hit_rate < 0.5`：缓存未生效
- `avg_children_per_parent > 5`：子块过多，调高 threshold

---

## 参考资料

- **LangChain Parent Document Retriever**：https://python.langchain.com/docs/modules/data_connection/retrievers/parent_document_retriever
- **论文**：《Lost in the Middle: How Language Models Use Long Contexts》（2023）
  - 结论：LLM 对长文档中间部分的注意力较弱，需要标注重点片段
- **内部文档**：
  - `06-数据模型设计.md`：Chunk 父子关系定义
  - `10-召回层设计.md`：当前召回逻辑
  - `11-重排层设计.md`：重排后接入点

---

## 附录：配置参数

新增配置项（`backend/app/config.py`）：

```python
# 子块扩展配置
CHILD_EXPANSION_ENABLED: bool = True  # 是否启用子块扩展
CHILD_SIMILARITY_THRESHOLD: float = 0.7  # 子块相似度阈值
MAX_CHILDREN_PER_PARENT: int = 5  # 每个父块最多保留的子块数
```
