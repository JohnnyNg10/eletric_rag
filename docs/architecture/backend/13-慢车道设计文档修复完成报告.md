# 慢车道设计文档修复完成报告

> 修复时间：2026-07-10
> 修复文档：13-慢车道设计.md、06-数据模型设计.md

---

## 修复清单

### ✅ 问题 1：`query_logs.strategy` 字段用途冲突（已修复）

**修改文件**：`13-慢车道设计.md` 第十一节

**修改内容**：
- ❌ 删除：`strategy` 字段存储工具调用序列
- ✅ 改为：使用 `expanded_queries` 字段存储各步骤查询文本
- ✅ 新增：建议扩展 `reasoning_steps` 字段（JSON）存储完整推理链路，包含工具名、参数、耗时、结果数

**影响**：避免字段用途冲突，`strategy` 字段保留原定义（提问优化策略：none/suggest/clarify_optional/clarify_required）

---

### ✅ 问题 2：缺少 `conversation_id` 字段说明（已修复）

**修改文件**：
- `13-慢车道设计.md` 第十一节
- `06-数据模型设计.md` 第 2.2.4 节

**修改内容**：
- ✅ 在慢车道设计文档中补充 `conversation_id` 字段说明
- ✅ 在数据模型文档中正式定义该字段：`VARCHAR(100)`，用于多轮对话关联
- ✅ 更新 SQL 建表语句，包含 `conversation_id` 字段及索引

---

### ✅ 问题 3：工具输出格式不统一（已修复）

**修改文件**：`13-慢车道设计.md` 第五节、第七节

**修改内容**：
- ✅ 明确 `retrieve_standard` 输出：`List[ChunkResult]`
- ✅ 明确 `retrieve_clause` 输出：`List[ChunkResult]`（长度为1的列表）
- ✅ 明确 `list_related_standards` 输出：`List[Dict]`（文档元信息，**不是 ChunkResult**）
- ✅ 在第七节信息聚合策略中说明：`list_related_standards` 不参与 chunk 聚合，仅作为中间信息传给 LLM

**影响**：消除输出格式歧义，明确聚合流程中的特殊处理

---

### ✅ 问题 4：工具参数命名一致性（已确认）

**检查结果**：
- `retrieve_standard` 参数：`standard_ids`（`List[str]`，可选）
- `retrieve_clause` 参数：`standard_id`（`str`，必填）

**结论**：命名一致，无需修改。`standard_ids` 用于多标准过滤（复数），`standard_id` 用于单标准定位（单数），语义清晰。

---

### ✅ 优化点 1：LLM 决策超时（已确认正确）

**检查结果**：文档已正确说明 LLM 决策超时 1.5s，并明确"与快车道充分性判断的超时策略保持一致"。

**结论**：无需修改 ✅

---

### ✅ 优化点 2：限流技术实现方式（已补充）

**修改文件**：`13-慢车道设计.md` 第十节

**修改内容**：
- ✅ 补充限流实现方式：Redis 滑动窗口计数器
- ✅ 补充 Key 格式：`ratelimit:slow_lane:{timestamp_second}`
- ✅ 补充限流粒度：全局限流（所有用户共享 10 QPS）
- ✅ 补充超限响应格式：HTTP 429 + JSON 错误提示

---

## 额外修复

### ✅ 数据库表定义完善（已补充）

**修改文件**：`06-数据模型设计.md` 第 2.2.4 节

**新增字段**：
1. `conversation_id VARCHAR(100)` — 会话ID（多轮对话）
2. `rerank_scores JSON` — 精排结果（快车道记录）
3. `sufficiency_result JSON` — 充分性判断结果（快车道记录）
4. `reasoning_steps JSON` — 慢车道推理步骤详情

**新增索引**：`idx_conversation_id`

**影响**：数据库表定义与实际使用完全对齐，后续实现可直接参照

---

## 修复后的文档状态

### 13-慢车道设计.md

- ✅ 工具输出格式明确，三个工具返回类型清晰定义
- ✅ 日志字段映射正确，使用 `expanded_queries` 和建议的 `reasoning_steps`
- ✅ 信息聚合策略补充特殊处理说明
- ✅ 限流技术实现细节完整
- ✅ `conversation_id` 字段补充说明

### 06-数据模型设计.md

- ✅ `query_logs` 表新增 4 个字段
- ✅ SQL 建表语句同步更新
- ✅ 索引列表同步更新
- ✅ 字段说明与慢车道设计文档对齐

---

## 验证清单

实现慢车道时，请确认以下内容：

### 代码实现
- [ ] 三个工具返回格式与文档一致
- [ ] `list_related_standards` 输出不参与 chunk 聚合
- [ ] 日志记录字段映射正确（避免误用 `strategy` 字段）
- [ ] 限流使用 Redis 滑动窗口，key 格式符合规范
- [ ] 支持 `conversation_id` 的传入和记录

### 数据库迁移
- [ ] 执行 ALTER TABLE 添加 `conversation_id`、`rerank_scores`、`sufficiency_result`、`reasoning_steps` 字段
- [ ] 创建 `idx_conversation_id` 索引
- [ ] 验证现有数据兼容性（新字段允许 NULL）

### 测试验证
- [ ] 慢车道查询完整流程测试
- [ ] 日志字段写入正确性验证
- [ ] 限流机制有效性测试（连续发送 >10 QPS，验证返回 429）
- [ ] 多轮对话场景验证（conversation_id 正确关联）

---

## 相关文档

- [13-慢车道设计.md](./13-慢车道设计.md) — 已修复
- [06-数据模型设计.md](./06-数据模型设计.md) — 已修复
- [13-慢车道设计文档检查报告.md](./13-慢车道设计文档检查报告.md) — 原始检查报告
