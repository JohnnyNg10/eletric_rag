# RAG系统功能提升建议

根据设计文档和当前实现状态的对比分析，整理出以下可以提升的功能点。

## 一、已实现的核心功能 ✓

### 1. 文档处理层
- ✓ PDF解析（MinerU集成，支持文字版和扫描版）
- ✓ 智能分块（父子块机制）
- ✓ 元数据提取（标准号、章节、条款识别）
- ✓ 文档分类（电压等级、专业分类）
- ✓ 图片处理（VLM图片描述生成）
- ✓ 表格识别与Markdown转换

### 2. 预处理层
- ✓ 术语标准化（同义词映射）
- ✓ 笼统度评估（LLM驱动）
- ✓ 澄清选项生成（KB引导）
- ✓ 查询重写（HyDE）
- ✓ 元数据提取（电压等级、专业分类）
- ✓ 指代消解（多轮对话支持）

### 3. 路由层
- ✓ 快慢车道路由决策
- ✓ 复杂度评估

### 4. 召回层（快车道）
- ✓ 三路召回（向量+关键词+HyDE）
- ✓ 混合检索（稠密+稀疏向量RRF融合）
- ✓ 父块召回 + 子块展开
- ✓ 元数据过滤

### 5. 重排层
- ✓ 两阶段重排（base → large）
- ✓ 召回充分性判断
- ✓ 二次检索（gap refinement）
- ✓ 图片链接注入

### 6. 生成层
- ✓ LLM答案生成（豆包Pro/通义千问）
- ✓ 引用标注提取
- ✓ 流式输出
- ✓ 多轮对话历史注入
- ✓ 图片展示（presigned URL）

### 7. 存储层
- ✓ 向量数据库（Qdrant，混合检索）
- ✓ 全文检索（Elasticsearch，BM25）
- ✓ 关系数据库（MySQL，元数据）
- ✓ 对象存储（MinIO，PDF/图片）
- ✓ 缓存层（Redis，四级缓存：embedding/召回/重排/生成）

### 8. 基础设施
- ✓ 模型自动下载
- ✓ Docker部署配置
- ✓ 日志记录（查询日志、澄清日志）

---

## 二、待实现/可提升的功能点

### A. 高优先级（P0 - 核心功能增强）

#### 1. 慢车道（多跳推理）优化【✓ 已实现框架，待增强】
**现状**：慢车道核心框架已完整实现（工具调用循环、最多3步推理、`retrieve_standard`/`retrieve_clause`/`list_related_standards`/`general_retrieval` 四种工具）
**建议增强方向**：
- 扩展工具集：增加标准版本对比、表格精确查询、公式检索等专用工具
- 优化决策 Prompt：提升工具选择准确率，减少无效调用
- 决策链路可视化：前端展示推理步骤和工具调用记录
- 图片链路完善：确保慢车道也能正确召回和展示图片（已修复基础 bug）

**收益**：
- 覆盖约10%的复杂查询场景（当前已支持）
- 进一步提升工具选择准确率和多跳推理质量

---

#### 2. 事实一致性校验【部分实现】
**现状**：validator.py已实现框架，但默认关闭（成本考虑）
**建议**：
- 优化验证Prompt，降低误报率
- 选择性校验：仅对关键数值、限值、条款引用进行校验
- 校验结果可视化（标注不一致的地方）
- 增加人工反馈机制，构建验证数据集

**收益**：
- 提升答案可信度，减少幻觉
- 满足"零臆测、可溯源、可校验"的核心定位

---

#### 3. 标准版本管理与有效性校验
**现状**：元数据中有版本字段，但未实现自动更新和废止提醒
**建议**：
- 定期爬取国家标准馆，检测标准更新
- 废止标准自动标记，检索时过滤或提示
- 版本替代关系建模（A标准被B标准替代）
- 前端展示标准状态徽章（有效/废止/即将废止）

**收益**：
- 确保数据合规性（100%有效标准）
- 避免引用已废止标准导致的合规风险

---

#### 4. 引用链路完整性增强
**现状**：已有引用标注提取，但缺少引用验证和跳转
**建议**：
- 引用编号点击跳转到原文位置
- 引用来源完整率检测（LLM生成的每个关键陈述是否都有引用）
- 引用粒度细化：具体到页码、条款号
- PDF原文高亮显示引用段落

**收益**：
- 提升可溯源性（引用来源完整率达到100%）
- 增强用户信任度

---

### B. 中优先级（P1 - 性能与体验优化）

#### 5. 性能优化【已有诊断报告，分阶段实施】
**现状**：快车道P95延迟约78秒，设计目标为 ≤3秒
**优化路径**（分阶段推进）：
- **阶段1（P0优化）**：消除重复重排（预期-24秒，31%提升）→ 目标54秒
- **阶段2（P1优化）**：修复category过滤失效（预期-7秒，9%提升）→ 目标47秒
- **阶段3（P2优化）**：模型量化（INT8 quantization，预期-10秒，13%提升）→ 目标37秒
- **阶段4（P3优化）**：异步I/O改造（Redis/MySQL/ES/Qdrant，预期-15秒+QPS提升2-3x）→ 目标22秒
- **阶段5（终极优化）**：召回策略简化、重排模型加速、生成并行化 → 目标 ≤5秒

**收益**：
- 近期（阶段1-2）：P95降至50秒以内
- 中期（阶段3-4）：P95降至25秒以内
- 长期（阶段5）：逼近设计目标3秒

**关联项**：与第6条"异步架构改造"合并为统一的性能优化计划

---

#### 6. 异步架构改造【已有详细设计，并入性能优化计划】
**现状**：存储层同步调用阻塞asyncio事件循环，是性能瓶颈之一
**优先级排序**：
- 优先改造Redis（高频调用，影响所有请求）
- 其次改造ES和Qdrant（召回瓶颈，每次查询必经）
- 最后改造MySQL和MinIO（低频调用）
- 参考文档：`27-异步改造分析.md`

**收益**：
- 并发吞吐提升2-3倍
- 与性能优化第5条阶段4协同，综合P95降至22秒以内

---

#### 7. 查询缓存优化
**现状**：已有五级缓存（L1 Embedding/L2 召回/L3 重排/L4 生成/语义缓存），语义相似查询缓存（SemanticCache，基于 Qdrant）已实现，但命中率和TTL策略可优化
**建议**：
- 监控各级缓存命中率，动态调整TTL
- 热点查询预加载
- 缓存淘汰策略优化（LRU → LFU）
- 语义缓存相似度阈值调优（当前 0.95，可根据实测数据调整）

**收益**：
- 缓存命中率从当前基线提升20-30%
- P50延迟降低40-50%

---

#### 8. 批量文档导入优化
**现状**：单文档导入可用，但批量导入耗时长
**建议**：
- 并行解析（多进程池）
- 批量向量化（batch embedding）
- 批量写入优化（Qdrant/ES batch API）
- 进度追踪和断点续传

**收益**：
- 批量导入速度提升3-5倍
- 支持增量更新（仅重新处理变更部分）

---

#### 9. 前端交互增强
**现状**：基础问答界面可用，但缺少高级功能
**建议**：
- 检索过程可视化（召回→重排→生成各阶段耗时）
- 多候选答案展示（Top-3 chunks对比）
- 引用原文侧边栏预览（hover显示完整段落）
- 查询历史和收藏功能
- 相关问题推荐（基于当前问题）

**收益**：
- 提升用户体验和专业感
- 增加用户留存和使用频率

---

### C. 长期优化（P2 - 算法与数据增强）

#### 10. 路由微调模型【已有完整设计】
**现状**：当前使用大模型进行路由决策，延迟约2-3秒
**建议**：
- 微调RoBERTa-wwm-ext编码器（多任务学习）
- 预测：快慢车道分类 + 澄清选项生成
- 推理延迟降至50-100ms
- 参考文档：`16-阶段C-路由微调模型设计.md`

**收益**：
- 路由延迟降低95%（3秒 → 100ms）
- 整体P95延迟再降3秒

---

#### 11. 检索质量评测体系
**现状**：缺少系统化的检索质量评估
**建议**：
- 构建测试集（100-200个标注query-chunk对）
- 自动化评测指标：
  - Top-5召回准确率
  - Top-3条款精准命中率
  - MRR（Mean Reciprocal Rank）
  - nDCG（Normalized Discounted Cumulative Gain）
- 每次模型更新后自动回归测试
- A/B测试框架（对比不同召回/重排策略）

**收益**：
- 量化检索质量，达到设计目标（Top-5≥92%，Top-3≥95%）
- 数据驱动的迭代优化

---

#### 12. 主动学习与数据飞轮
**现状**：用户反馈未形成闭环
**建议**：
- 收集badcase（用户标记"答案无关"/"答案错误"）
- 人工标注review队列
- 定期重训练/微调模型（embedding/rerank/路由）
- 自定义澄清输入追踪（数据飞轮，参考`25-方案C`）

**收益**：
- 持续提升检索和生成质量
- 适配领域特定查询模式

---

#### 13. 多模态增强（电力标准专用优化）
**现状**：图片检索已实现（VLM描述 + 图号关联 + 图片伴随召回），VLM Prompt 已针对电力标准优化（✓ 2026-07-24完成）
**建议**：
- **表格图结构化提取**（最实用）：OCR + 表格解析 → 提取表头、数值、单位，支持表格数值精确查询
- **图片类型自动分类**：流程图/电路图/参数表图/示意图差异化处理
- **图文融合索引**：图片上下文扩展（包含图注和周边文字）
- **图片质量评分**：根据清晰度、引用次数、相关性排序
- **VLM 模型升级**：测试更强的专业 VLM（Qwen2-VL、Vary、LLaVA-Next）

**不建议**：
- ~~CLIP embedding~~：CLIP 主要训练于自然图像，对电力工程图纸的理解能力弱于 VLM 文字描述，不适合本场景

**收益**：
- 表格图数值查询准确率大幅提升（当前无法精确查表格）
- 复杂图纸描述质量提升

---

#### 14. 知识图谱增强
**现状**：当前基于向量检索，缺少结构化知识
**建议**：
- 抽取实体关系（标准号-条款号-概念）
- 构建轻量级知识图谱（Neo4j）
- 混合检索：向量召回 + 图谱路径查询
- 自动补全和关联推荐

**收益**：
- 提升复杂关系推理能力
- 支持"XX标准和YY标准在ZZ方面有何异同"类对比问题

---

### D. 运维与监控（P1）

#### 15. 数据一致性与类型安全【✓ 部分已修复】
**现状**：今日发现多处数据类型不一致导致的bug
**已修复问题**：
- Qdrant `doc_id` 类型不一致：写入时为 int，删除时传 str，导致文档删除失败（2026-07-24 已修复）
- 慢车道 `ChunkResult → RerankResult` 转换缺少图片字段，导致图片不展示（2026-07-24 已修复）
- `ImageInfo.page_number` 字段接收 `None` 导致 Pydantic 校验失败（2026-07-24 已修复）

**建议增强**：
- 建立存储层类型契约测试（int vs str, None vs 0, Optional 字段处理）
- 增加端到端集成测试，覆盖慢车道图片展示链路
- Pydantic schema 增加更宽松的验证（使用 `field_validator` 自动类型转换）
- 添加类型检查工具（mypy）到 CI 流程

**收益**：
- 减少因类型不一致导致的运行时错误
- 提前在开发阶段发现类型问题

---

#### 16. 监控告警体系
**现状**：基础日志记录，但缺少实时监控
**建议**：
- 核心指标监控：
  - QPS、P50/P95/P99延迟
  - 各阶段耗时分布（预处理/召回/重排/生成）
  - 缓存命中率
  - 模型推理耗时
  - 错误率和超时率
- 告警规则（根据当前实际水位设置，随优化进展逐步收紧）：
  - **当前阶段**：P95延迟 > 120秒（异常慢）
  - **优化后阶段1**：P95延迟 > 80秒
  - **优化后阶段2**：P95延迟 > 40秒
  - **最终目标**：P95延迟 > 5秒
  - 错误率 > 1%
  - QPS突降 > 30%
- 可视化面板（Grafana）

**收益**：
- 及时发现性能退化和异常
- 满足99.9%可用性SLA

---

#### 17. 成本优化
**现状**：LLM调用成本较高，语义缓存已实现但覆盖率有限
**建议**：
- 扩大语义缓存覆盖（调整相似度阈值，增加命中率）
- 模型降级策略（简单query用小模型，复杂query用大模型）
- Prompt压缩（减少上下文token数，移除冗余示例）
- 本地部署开源模型（降低API成本，适合高频场景）

**收益**：
- LLM调用成本降低30-50%
- 提升系统经济性

---

#### 18. LLM Prompt 质量保障与迭代
**现状**：生成层 Prompt 已包含图片标注提示（✓ 2026-07-24已优化），VLM Prompt 已针对电力标准重写（✓ 2026-07-24已优化），但缺少 Prompt 版本管理和 A/B 测试
**建议**：
- **Prompt 版本管理**：将所有 system prompt 从代码抽离到配置文件，支持版本回滚
- **Prompt A/B 测试**：同时运行两个版本的 Prompt，对比生成质量（引用完整率、事实准确率、用户满意度）
- **领域专用指令库**：针对不同查询类型（条款查询/参数查询/对比查询/图表查询）使用不同的 system prompt

**收益**：
- 快速迭代和验证 Prompt 改进效果
- 提升不同查询类型下的答案质量

---

#### 19. 文档导入质量校验
**现状**：文档导入后无自动质量检查，可能存在解析错误、分块不合理、元数据缺失
**建议**：
- **自动质量检查**：
  - 分块长度分布检测（异常长/短的 chunk）
  - 元数据完整性检查（标准号/章节/条款是否缺失）
  - 图片描述质量评分（VLM 输出是否为空或过短）
  - 表格识别率统计（多少表格被正确解析）
- **导入报告生成**：每次导入后输出质量报告（chunk 数、图片数、警告项）
- **异常 chunk 人工审核**：质量分数低的 chunk 标记供复查

**收益**：
- 及早发现文档解析问题
- 提升入库数据质量，减少召回噪音

---

#### 20. 标准间引用关系图谱
**现状**：标准之间存在大量引用关系（"应符合 GB XXX 的规定"），但未结构化，慢车道多跳推理靠 LLM 自行判断
**建议**：
- **引用关系自动抽取**：用 NER + 规则从正文中提取标准引用
- **标准关系图谱**：节点为标准/条款，边为引用/替代/补充关系
- **跨标准推理增强**：在慢车道中利用图谱路径辅助工具选择
- **前端关系可视化**：展示标准引用树

**收益**：
- 提升"XX 标准和 YY 标准的关系"类查询准确率
- 慢车道工具选择更精准，减少无效步骤

---

#### 21. 答案质量自动评分与反馈闭环
**现状**：用户可以点赞/点踩，但未形成数据闭环，低质量答案无法自动发现
**建议**：
- **答案质量自动评分**：
  - 引用完整率（答案的关键陈述是否都有引用编号）
  - 引用精准度（引用 chunk 与答案的语义相关性）
  - 事实一致性（答案是否与引用原文矛盾）
- **低分答案自动标记**：质量分 < 60 进入人工审核队列
- **反馈闭环**：用户点踩 → 标记原因 → 定期 review badcase → 更新 Prompt/模型

**收益**：
- 主动发现质量问题，不完全依赖用户反馈
- 数据驱动的持续优化

---
**收益**：
- LLM调用成本降低30-50%
- 提升系统经济性

---

## 三、优先级总结

### 立即着手（1-2周）
1. **数据一致性修复**（✓ 已完成大部分，2026-07-24）
2. **性能优化阶段1-2**（消除重复重排、修复过滤失效）→ P95降至50秒
3. **监控告警体系**（生产环境必备，设置合理的告警阈值）
4. **慢车道工具优化**（当前已实现，增强决策 Prompt 和工具集）

### 近期规划（1-2月）
5. **事实一致性校验优化**（提升可信度，选择性校验降低成本）
6. **标准版本管理**（确保合规性，自动检测废止标准）
7. **引用链路增强**（提升溯源性，支持引用点击跳转）
8. **异步架构改造阶段1**（Redis 异步化）→ QPS 提升至 100+
9. **表格图结构化提取**（OCR + 解析，支持表格数值查询）

### 中期目标（3-6月）
10. **检索质量评测体系**（量化优化效果，构建测试集）
11. **路由微调模型**（降低路由延迟 3s → 100ms）
12. **Prompt 质量保障**（版本管理 + A/B 测试）
13. **文档导入质量校验**（自动检测解析问题）
14. **前端交互增强**（过程可视化、引用预览）
15. **标准引用关系图谱**（抽取标准间引用，辅助跨标准推理）

### 长期探索（6月+）
16. **多模态增强**（VLM 模型升级、图片分类、图文融合）
17. **知识图谱增强**（Neo4j + 混合检索）
18. **主动学习闭环**（badcase 驱动的持续优化）
19. **答案质量自动评分**（引用完整率、事实一致性评分）
20. **成本优化**（模型降级策略、本地模型部署）

---

## 四、关键设计文档参考

- **慢车道设计**：`docs/architecture/backend/13-慢车道设计.md`
- **性能优化**：`docs/architecture/backend/26-快车道延迟分析与优化方案.md`
- **异步改造**：`docs/architecture/backend/27-异步改造分析.md`
- **路由微调**：`docs/architecture/backend/16-阶段C-路由微调模型设计.md`
- **缓存设计**：`docs/architecture/backend/19-缓存层设计.md`
- **多轮对话**：`docs/architecture/backend/20-多轮对话设计.md`
- **图片检索**：`docs/architecture/modules/29-图片检索增强设计.md`

---

**总结**：当前系统核心 RAG 流程已完整实现（预处理→路由→快慢车道召回→重排→生成→多模态展示），慢车道工具调用循环、语义缓存、图片检索均已上线。

**当前主要问题**：
1. **性能瓶颈**：P95 延迟 78 秒，距离设计目标（≤3秒）差距较大，需分阶段优化
2. **数据一致性**：类型不一致导致的运行时错误（✓ 大部分已修复）
3. **多模态链路**：图片描述质量、表格结构化提取有提升空间

**优化重点**：
- **短期**：性能优化（重排、异步化）、监控告警、数据质量
- **中期**：检索质量评测、Prompt 迭代、标准关系图谱
- **长期**：知识图谱、主动学习、答案质量自动评分

建议按 P0→P1→P2 优先级逐步推进，优先解决性能和数据质量问题，再深化检索和生成质量。
